# Kubernetes Deployment for 金融智能客服 API
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-agent-api
  namespace: finance-agent
  labels:
    app: finance-agent
    component: api
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: finance-agent
      component: api
  template:
    metadata:
      labels:
        app: finance-agent
        component: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: finance-agent
      containers:
      - name: api
        image: finance-agent:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: finance-agent-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: finance-agent-secrets
              key: redis-url
        - name: QDRANT_URL
          value: "http://qdrant-service:6333"
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: finance-agent-secrets
              key: openai-api-key
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: finance-agent-secrets
              key: secret-key
        - name: LOG_LEVEL
          value: "INFO"
        - name: CORS_ORIGINS
          value: "https://chat.example.com"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: finance-agent-service
  namespace: finance-agent
  labels:
    app: finance-agent
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: finance-agent
    component: api
---
# Horizontal Pod Autoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: finance-agent-hpa
  namespace: finance-agent
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: finance-agent-api
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
---
# PodDisruptionBudget
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: finance-agent-pdb
  namespace: finance-agent
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: finance-agent
      component: api
---
# ServiceAccount
apiVersion: v1
kind: ServiceAccount
metadata:
  name: finance-agent
  namespace: finance-agent
---
# ConfigMap for application config
apiVersion: v1
kind: ConfigMap
metadata:
  name: finance-agent-config
  namespace: finance-agent
data:
  MEMORY_TYPE: "hybrid"
  INTENT_TYPE: "hybrid"
  WINDOW_SIZE: "10"
  SUMMARY_THRESHOLD: "20"
  VECTOR_WEIGHT: "0.7"
  RATE_LIMIT_REQUESTS_PER_MINUTE: "60"
